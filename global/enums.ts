/**
 * @description Data type of the filter
 * @enum DataType
 */
export enum DataType {
	Ustensil = 'ustensils',
	Ingredient = 'ingredients',
	Season = 'seasons',
	Allergen = 'allergens',
	MealType = 'mealTypes',
	DishType = 'dishTypes',
}
